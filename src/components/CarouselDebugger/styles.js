import classNames from 'classnames/bind';
import styles from './styles.scss';

const cx = classNames.bind(styles);

export const container = cx('container');
export const formGroup = cx('form-group');
